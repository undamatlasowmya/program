//print n1 to n2 odd  numbers except third number in the range.read1 and n2 from the user.
/*eg: n1=10,n2=20, then output would be: 11,13,17,19
explanation: 15 is skipped because it is 3rd number in the rangeas per the question
eg2:n1=5,n2=10 then output would be: 5,7
explanatio: 9 is skipped because it is a 3rd number in the range as per the qustion*/
#include<stdio.h>
#include<conio.h>
main()
{
	int n1,n2;
	printf("Enter the numbers\n");
	scanf("%d%d",&n1,&n2);
	for(;n1<=n2;)
	{	
	if(n1%2!=0)n
	;
    }
    printf("%d\n",n1);
    n1=n1+2;
}
